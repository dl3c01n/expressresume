<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="../assets/css/index.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
</head>
<body>
    <div class="container text-center" style="margin-top: 100px;">
        <h1 id="loadingH1"><span><i class="fa fa-cogs"></i></span> <%= ` > ${mockData.name} ${mockData.firstName}` %>  - <%= mockData.username %></h1>
        <div class="row">
            <div class="col-6 offset-3 text-center">
                <h3><%= `> ${mockData.age} ans` %></h3>
                <h5>> <%= mockData.job %></h5>
            </div>
        </div>
        <div class="row" style="margin-top: 100px;">
            <div class="col-lg-7 mx-auto">
                <ul class="timeline">
                    <h2> > Études</h2>

                    <% mockData.education.map((e) =>{ %>
                        <li class="timeline-item rounded ml-3 p-4 shadow">
                            <div class="timeline-arrow"></div>
                            <h2 class="h5 mb-0"><%= e.school %></h2><span class="small text-gray"><i class="fa fa-clock-o mr-1"></i><%= e.endDate %></span>
                            <p class="text-small mt-2 font-weight-light"><%= `${e.exam} - ${e.level}` %></p>
                        </li>
                    <% }) %>
                    </ul>
                    <ul class="timeline" style="margin-top: 100px;">
                    <h2> > Expériences</h2>
                    <% mockData.jobs.map((j) =>{ %>
                        <li class="timeline-item rounded ml-3 p-4 shadow">
                            <div class="timeline-arrow"></div>
                            <h2 class="h5 mb-0"><%= j.company %></h2><span class="small text-gray"><i class="fa fa-clock-o mr-1"></i><%= j.date %></span>
                            <p class="text-small mt-2 font-weight-light"><%= `${j.job} - ${j.mission}` %></p>
                        </li>
                    <% }) %>
                </ul>
            </div>
        </div>
        <div class="row" style="margin: 80px 0;">
            <div class="row" style="margin-bottom: 50px">
                <div class="col-6 offset-3">
                    <h2>> Skills</h2>
                </div>
            </div>
            <% for(let i = 0; i < mockData.skills.length; i++){ %>
                <div class="col-3 text-center">
                        > <%= mockData.skills[i] %>
                </div>
                <%} %>
        </div>
     </div>
</body>
</html>